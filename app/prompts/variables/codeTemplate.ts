export const codeTemplate = `const { ... } = jscad.primitives
const { ... } = jscad.transforms
const { ... } = jscad.colors
const { ... } = jscad.booleans

const main = () => {
  // Define your geometry here

  return [
    // Return your geometry here
  ]
}

return main()`
